# MSDS 410 — Assignment 4: Model Building and Diagnostics (Ames Housing)

## What it does
Builds out multiple candidate linear regression models on the Ames Housing dataset,
extending the variable-selection and diagnostic work from Assignments 1 and 3.
Covers model comparison, residual diagnostics, and identification of influential
observations/multicollinearity checks in preparation for a finalized housing price model.

## Techniques/libraries used
- R: `dplyr`, `tidyr`, `readr`, `knitr`, `corrplot`
- Multiple linear regression (`lm`), residual diagnostics, correlation analysis

## Data note
Ran against the Ames Housing dataset (`ames_housing_data.csv`) — not included (large
dataset file, course-provided, same as Assignments 1 and 3).

## Note
An earlier draft version of this notebook (`Assignment4 copy.Rmd`) existed alongside the
final submission — only the final version is included here.
