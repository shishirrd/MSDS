# MSDS 410 — Assignment 7: Logistic Regression Basics (ICU Data)

## What it does
Introduces logistic regression using an ICU patient dataset, predicting a binary outcome
from patient characteristics. Covers data exploration, model fitting, coefficient
interpretation in log-odds/odds-ratio terms, and basic model evaluation.

## Techniques/libraries used
- R: `readxl`, `ggplot2`, `dplyr`, `knitr`
- Logistic regression (`glm` with binomial family)

## Data note
Ran against the ICU dataset (`icu.csv`/`icu.xlsx`) — not included (small course-provided
dataset with accompanying data dictionary).

## Note
An earlier draft version of this notebook (`Assignment7_AI.Rmd`) existed alongside the
final submission — only the final version is included here.
