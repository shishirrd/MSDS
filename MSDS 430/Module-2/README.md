# MSDS 430 — Module 2: Booleans and Conditional Logic

## What it does
Notebook exercises on the `bool` data type, Boolean expressions, and conditional
statements (`if`/`elif`/`else`) — building toward selection-based program control flow.

## Files
- `Deshpande - Module_2_Python_Assignment.ipynb`

## Techniques/libraries used
- Boolean expressions and comparisons
- Conditional branching (`if`/`elif`/`else`)
- Python standard library modules (`math`, `random`)
